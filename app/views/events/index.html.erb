<p id="notice"><%= notice %></p>

<h1>Post Events</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Narasi</th>
      <th>Tanggal Dilaksanakan</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <% if event.dilaksanakan < Date.today %>
        <tr>
          <td><%= event.title %></td>
          <td><%= event.narasi %></td>
          <td><%= event.dilaksanakan.strftime("%m/%d/%Y") %></td>
          <td><%= link_to 'Show', event %></td>
          <td><%= link_to 'Edit', edit_event_path(event) %></td>
          <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<h1>Future Events</h1>

<table>
  <thead>
  <tr>
    <th>Title</th>
    <th>Narasi</th>
    <th>Tanggal Dilaksanakan</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
    <% if event.dilaksanakan > Date.today %>
      <tr>
        <td><%= event.title %></td>
        <td><%= event.narasi %></td>
        <td><%= event.dilaksanakan.strftime("%m/%d/%Y") %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>

<br>

<br>
<%= link_to 'New Event', new_event_path %>

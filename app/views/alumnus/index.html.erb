<p id="notice"><%= notice %></p>

<!--
<table>
<thead>
    <tr>
    <th>Nama</th>
    <th>Kelas</th>
    <th colspan="3"></th>
    </tr>
</thead>

<tbody>
    <% @alumnus.each do |alumnu| %>
    <tr>
        <td><%= alumnu.nama %></td>
        <td><%= alumnu.kelas %></td>
        <td><%= link_to 'Show', alumnu %></td>
        <td><%= link_to 'Edit', edit_alumnu_path(alumnu) %></td>
        <td><%= link_to 'Destroy', alumnu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
</tbody>
</table>
-->

<div id='alumni-main'>
    <div id='alumni-filter'>
        <h2>Filter</h2>
        <hr>
        <div>
            <p>Kelas 1</p>
            <p>Kelas 2</p>
            <p>Kelas 3</p>
        </div>

        <div>
            <select>
                <option>K-01</option>
                <option>K-01</option>
            </select>
            <select>
                <option>K-01</option>
                <option>K-01</option>
            </select>
            <select>
                <option>K-01</option>
                <option>K-01</option>
            </select>
        </div>
    </div>

    <div id='alumni-list'>
        <% @alumnus.each do |alumnu| %>
            <div class='alumni-span'>
                <div class='alumni-photo'>
                    <%= image_tag(alumnu.photo.foto.thumb.url) %>
                </div>
                <div class='alumni-data'>
                    <h1><%= alumnu.nama %></h1>
                    <h2 style="display: inline; font-weight: 400">Kelas:&nbsp;</h2>
                    <h2 style="display: inline;"><%= alumnu.kelas %></h2>
                    <div class='alumni-controller'>
                        <p><%= link_to 'Show', alumnu %></p>
                        <p><%= link_to 'Edit', edit_alumnu_path(alumnu) %></p>
                        <p><%= link_to 'Destroy', alumnu, method: :delete, data: { confirm: 'Are you sure?' } %></p>
                    </div>
                </div>
            </div>
        <% end %>
    </div>

    <%= link_to 'New Alumnu', new_alumnu_path %>
</div>
